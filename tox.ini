[tox]
envlist = py39
skipsdist = True

[testenv]
deps =
    pytest
    coverage
    pytest-cov
    -rrequirements.txt
commands = pytest --cov=debait --cov-report=xml --cov-config=tox.ini --cov-branch

[coverage:run]